# TextArea Input

<!-- STORY -->

```jsx
import { Form, TextArea, Debug } from 'informed';

const TextAreaInput = () => (
  <Form>
    <TextArea name="bio" label="Bio:" />
    <button type="submit">Submit</button>
    <Debug values />
  </Form>
);
```
